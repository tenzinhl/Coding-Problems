#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define INF (int) 2e9

int main()
{
	auto p1 = make_pair(5, 5);
	auto p2 = make_pair(6, 3);
	if (p2 < p1) cout << "true" << endl;
	
	pair<int, int> plist[4] = {
		make_pair(1, 1),
		make_pair(2, 1),
		make_pair(2, 2),
		make_pair(3, 1)
	}
	
	cout << "How pairs handle being fed to comparison function in sort(): \n";
	cout << "array before: "
	
	return 0;
}
