#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define INF (int) 2e9
#include <sys/socket.h> // Compilation will terminate if you try to include this
// and the compiler can't find it.
void solve() {
	
}

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(0);
	
	int t;
	cin >> t;
	for (int i = 1; i <= t; i++) {
		cout << "Case #" << i << ": ";
		solve();
	}
	return 0;
}

