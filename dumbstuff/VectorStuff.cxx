#include <bits/stdc++.h>
using namespace std;
#define ll long long


int main()
{	
	// vector<int> v;
	// v[0].push_back(1);
	// v[0].push_back(2);
	// cout << v[0];
	
	/* I'm such a dumbass. the original declaration: vector<int> con[LIM]; declares
	 * c-style array con of vector<int>'s lol. omg. */
	return 0;
}

