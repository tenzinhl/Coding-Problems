#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main()
{
	int a, b;
	a = 4;
	b = 2;
	if ((a == 5 or b > 0) and b <= 2) // so parantheses and conditional work as you would expect, it evaluates parantheses first.
		cout << "hi";
	return 0;
}

