#include <bits/stdc++.h>
using namespace std;
#define ll long long

// Okay, so returning non-zero number results in runtime error. How about 0?
// WHEREAS: Returning 0 results in "Wrong Answer" result. So that's cool.
// The code submission system let's you build in exit codes if you're so inclined.
// And 0 is special in that it will give you WA, as it assumes that means your
// code successfully executed.
int main()
{
	return 0;
}

