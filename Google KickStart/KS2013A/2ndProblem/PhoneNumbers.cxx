#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define INF (int) 2e9

const string countWords = {"", "", "double", "triple", "quadruple", "quintuple",
	"sextuple", "septuple", "octuple", "nonuple", "decuple"};
	
void solve() {
	int n;
	cin >> n;
	string f;
	cin >> f;
	for (int i = 0; i < n; i++) {
		
	}
}

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(0);
	
	int t;
	cin >> t;
	for (int i = 1; i <= t; i++) {
		cout << "Case #" << i << ": ";
		solve();
	}
	return 0;
}

